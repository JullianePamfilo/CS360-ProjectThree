<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center_horizontal">

    <!-- I use this TextView to display the current SMS permission status -->
    <TextView
        android:id="@+id/textPermissionStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Checking permission..."
        android:textSize="18sp"
        android:layout_marginBottom="24dp"
        android:textAlignment="center"
        android:textColor="#000000" />

    <!-- This is the button I tap when I want to request SMS permission -->
    <Button
        android:id="@+id/buttonRequestSms"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Request SMS Permission"
        android:padding="12dp"
        android:textSize="16sp"
        android:backgroundTint="#6200EE"
        android:textColor="#FFFFFF" />
</LinearLayout>
